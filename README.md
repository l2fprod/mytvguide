# mytvguide — TV schedule visualizer

Simple web app that visualizes channels and programmes as a horizontal timeline.

```bash
npm install
npm run generate-data
npm run dev
# open http://localhost:5173

# to build a static SPA bundle:
npm run build
```
